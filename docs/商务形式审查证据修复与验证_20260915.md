# 商务形式审查证据修复与验证

日期：2026-09-15（北京时间）。状态：已完成代码修复、定向验证及后端发布。

## 本次范围

依据用户指定的问题，修复明确截止日期漏识别、正文附件引用截断、同页附件边界、签字坐标、分项金额提取与附加费用、附件编号与标题冲突。未对其他审计问题开展新一轮整改。

处理依据不足时继续显示待复核，不通过猜测补齐金额或签字。OCR 原文、上传文件、既有人工结论与历史报告均不在本次改写范围内。本次使用已保存样本离线验证，没有调用全平台分析重跑接口。

## 改动及定向验证

| 问题 | 修复方式 | 已存样本验证结果 |
|---|---|---|
| 明确截止日期漏识别 | 补充“提交响应文件截止时间”等字段；沿用更正关系、冲突待复核与自然日比较 | 2026-219 提取为 2026-08-18；九亭提取为 2026-08-11。未改成一律取最早或最晚 |
| 正文附件引用截断落款 | 区分“此表合计须与附件2一致”等正文与独立附件标题，保留引用后的落款 | 海鼎分项报价落款 2026-08-10、竺海落款 2026-08-20 恢复定位，日期核验通过 |
| 同页附件标题被分类为正文 | 签章定位复用临时附件边界视图；明确编号的“响应文件偏离表”仍作为真实标题；保留页码与坐标 | 同一正文块中的附件8、附件9分别切开；正文引用不切开；原有双签要求仍检查两处 |
| 青白附件匹配错误 | 同编号不能覆盖明确标题冲突；匹配时规范化标题编号和格式后缀；防止“一 24 公司”等短片段被映射为报价表 | 报价一览表定位至第16页；分项报价表为第17—18页；偏离表为第19页，三项日期通过 |
| 空签字被正文判通过 | 将版面 xyxy 坐标转换为几何计算使用的 xywh；检测位置仍保留其 xywh 格式；禁止几何匹配失败后再用全文邻行兜底；同一签字证据不能填充两处签字位 | 辛恩、星淼比选保证书不再用标题、正文认定已签字，改为待复核；真实相邻姓名与签字检测位置仍能识别 |
| 型号数字被当单价 | 金额单元格使用独立完整解析器，支持元、万元、亿元；不修改通用数字函数，不从型号中提取子数字 | 骑士勋章由 2062×402 的错误计算恢复为 2062×455＝938210，检查通过 |
| 合价表头及合并单元格错位 | 识别“含税合价”和“频次”；优先明确列头，仅在数量、单价、总价列存在唯一一致位移时恢复列位置，不按算术吻合程度猜列 | 池金合价汇总恢复为 394926，通过；星淼汇总恢复为 518800，但仍有“-”金额行，因此保留待复核 |
| 金额行丢失仍判合计错误 | 序号明确而金额未提取的行进入未解析明细；只要存在影响汇总的未解析行，部分求和不能作为确定失败依据 | 南洋万邦、迅傲、竺海、奥软由确定失败改为待复核，保留未解析行和已取金额供人工核对 |
| 税费及管理费漏计 | 识别明确独立列出的附加费用金额；“已含税费”备注不重复加入；费用金额不唯一时待复核 | 艾杭 436000＋43600＝479600，通过 |
| 真实错误保护 | 完整可靠数据继续执行原算术校验 | 海鼎 419200 与 479200 的差额仍报错；舒汇 723000 与 529500 的矛盾仍报错 |

表中页码为上传 PDF 的物理页码，从 1 开始。数字“恢复”指分析时正确解析原文，不是修改 OCR 存储内容。

## 测试记录

- 新增 19 个回归测试，覆盖日期更正/冲突、正文引用、同块多附件、标题优先、坐标归属、单签不得复用为双签、金额单位、合并单元格、缺行、税费、真实算错等情况。
- 平台自动发现单测：187 项全部通过。
- 待发布镜像使用断网容器执行同一套 187 项测试，全部通过；没有依赖线上数据库或 OSS 来伪造测试成功。
- 镜像定向回放：10 份分项报价输入、5 份签章/附件输入、2 份招标截止日期输入。使用历史保存的 OCR，以及已有人工工作副本的 `effective_content`，与线上输入选择方式一致。通过断网限制，回放没有访问平台接口或写入结果。
- 两个真实金额矛盾样本仍失败；两份原件空签样本不再错误通过；指定错误附件定位与跨页落款均恢复。
- 未进行全量项目重跑或逐页重新人工鉴定。其他审计问题及尚不完整的 OCR 证据仍可能产生待复核。

测试代码：[test_review_evidence_repairs.py](/root/project_XTJS/project-XTJS/tests/test_review_evidence_repairs.py)。

## 发布与历史结果

- 后端已切换到 `project-xtjs:review-evidence-fix-20260915`，启动时间为 2026-09-15 02:27:21。
- 核对了 7 个发布源码文件的 SHA-256，运行容器与已测试源码一致。
- 后端健康接口 HTTP 200、状态 healthy；前端入口 HTTP 200。前端与 vLLM 的启动时间未变化。
- 发布前后 17 个有效项目的输入版本、状态一致；20 条结果记录的对象引用、输入版本、更新时间一致。没有批量改写结果或清除历史告警。
- 历史页面仍显示原保存结果；用户手动重新检查后，才会生成采用新规则的结果。人工材料仍有缺损或歧义时会继续保留待复核。
- Compose 在重建主服务时，自动移除了几个使用同一 app 服务标签的旧备用/验证容器。旧镜像及 `project-xtjs_ocr_runtime`、`project-xtjs_app_logs` 持久卷均保留；本次回退依赖旧镜像，不宣称保留了这些旧容器的临时文件层。

## 发布与回退命令

本次采用单服务镜像覆盖，没有更改现有 Compose 基础文件。重新部署时须包含最后的镜像覆盖文件。

```bash
cd /root/project_XTJS/project-XTJS
docker compose -p project-xtjs \
  -f docker-compose.yml -f docker-compose.gpu.yml \
  -f docker-compose.lean.yml -f docker-compose.vllm.yml \
  -f /data/llm_cpu_gpu_poc/experiments/review_repairs_20260915/compose-release.yml \
  up -d --no-deps --no-build app
```

如需回退，将最后的 `compose-release.yml` 换成同目录的 `compose-rollback.yml`，使用保留的 `project-xtjs:review-minimal-fix-20260914` 镜像。执行前应确认没有正在进行的上传、OCR 或分析任务。

## 留存证据

- [镜像单测记录](/data/llm_cpu_gpu_poc/experiments/review_repairs_20260915/image-tests.log)
- [镜像定向回放记录](/data/llm_cpu_gpu_poc/experiments/review_repairs_20260915/image-replay.log)
- [定向验收结果](/data/llm_cpu_gpu_poc/experiments/review_repairs_20260915/acceptance.json)
- [发布源码哈希](/data/llm_cpu_gpu_poc/experiments/review_repairs_20260915/release-source.json)
- [发布后健康状态](/data/llm_cpu_gpu_poc/experiments/review_repairs_20260915/health-after.json)

原始回放内容与发布前后结果引用快照留存在本机实验目录。本次未向 GitHub 推送代码或样本。
