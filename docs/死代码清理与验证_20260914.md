# 死代码清理与验证（2026-09-14）

已检查 XTJS 后端应用、运维工具及前端源码，并清理能够确认不可达或没有调用方的代码。清理以本次开始时的工作区为基线，保留此前尚未提交的业务修复。

本次共涉及 27 个既有源文件，净减少 **1710 行**。业务样本、接口定义和前端构建对照未发现行为变化。清理已写入源码；本次未切换线上镜像，未重启服务，未更新线上项目结果。

## 清理内容

| 类别 | 清理内容 | 确认依据 |
| --- | --- | --- |
| 不可达旧实现 | `ConsistencyChecker.compare_raw_data` 返回结构化引擎结果后的254行旧实现，以及仅供该实现调用的辅助方法、常量。 | 主入口在这些语句前无条件返回。新引擎仍引用的正文、填空和附件定位方法保留。 |
| 永不执行的分支 | 分项报价摘要中两个 `if False` 分支，共12行。 | 条件恒假且无 `else`，其中引用的未定义变量实际上从不执行。未调整分项报价判断。 |
| 无调用的后端方法 | 共66个函数／方法，涉及旧 Swagger 查询、批量结果写入辅助、旧预览精修、旧名称候选、旧费率判断及分析辅助方法。 | 对应用、工具、测试做全局引用和动态调用检查；删除后继续检查仅由死方法引用的下游方法。 |
| 无引用的后端常量 | 10个旧模板／名称规则常量。 | 仅被本次删除的旧方法使用，当前引擎无引用。 |
| 导入与局部变量 | 6个未使用导入项及7个无用途局部变量绑定。 | 静态检查和逐处核对。报告工具中的 `range_map.setdefault(...)` 保留执行，只移除未使用的赋值；OCR实际日志字段保留。 |
| 无调用的前端封装 | 25个未被页面、组件或测试导入／调用的请求封装。 | 全局引用检查，无命名空间动态调用；对应后端 HTTP 接口保留。 |
| 前端常量与素材 | 未使用的 `DOCUMENT_SCOPE_OPTIONS` 和 React 初始示例图标 `src/assets/react.svg`。 | 源码、样式、HTML均无引用；仍被页面使用的素材保留。 |

主要涉及文件：

- 后端模板及审查：`app/service/analysis/compliance/consistency.py`、`verification.py`、`reasonableness/float_rate.py`、`manual_review/business_bid_format.py`。
- 后端入口与工具：`app/main.py`、`app/router/postgresql.py`、`app/router/postgresql_batch.py`、`app/service/postgresql_service.py`、`tools/export_project_reports_from_pg.py`。
- 前端：`src/lib/xtjsApi.js`、`src/lib/authApi.js`、`src/utils/formatters.js`。

没有把“前端没有调用”等同于“后端接口可删除”。FastAPI路由、Pydantic验证器、包导出、当前引擎复用的方法、历史数据兼容路径，以及未能排除外部运维调用的公开服务方法均保留。检查结果不等于对所有动态使用方式做出了数学上的完备证明。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| Python静态检查 | `F401/F811/F821/F822/F841` 全部通过；后端应用和工具均可解析。 |
| 前端 lint、构建 | 全部通过。清理后 `dist` 的文件及逐文件 SHA-256 与此前发布产物完全一致。 |
| 平台后端单测 | **168项通过**。未将独立可选实验测试计入平台单测。 |
| 浏览器回归测试 | **18项通过**，覆盖项目切换、加载失败、异步归属、证据及缺附件预览等。 |
| 路由及文档契约 | 清理前后 **86条注册路由、65个OpenAPI路径**及完整OpenAPI内容一致。 |
| 保留函数结构核对 | 2565个保留函数的AST一致。其余结构差异仅涉及不可达代码、未使用局部变量和必要的测试调整。 |
| 业务回放 | 位育中学4份、2026-249共3份，清理前后各运行一次。完整性、模板一致性、签章日期、偏离、限价、分项报价共 **42组结果逐字段一致**，仅排除执行耗时字段。 |

业务回放使用同一依赖环境、相同原始OCR输入和相同CPU配置，均在断网的临时容器中执行，不访问数据库或OSS，不产生新OCR，也不持久化项目结果。2026-249佛山显扬、至和益各一条真实主动负偏离继续命中。

回放中的偏离检查采用相同的商务单文档输入，是用于验证代码清理前后等价的控制实验；线上商务＋技术联合输入及今日项目结论沿用此前重跑报告，本次未重新生成线上结果。

一个OpenAPI测试原先模拟 `app.main.PostgreSQLService`，删除未使用导入后该模拟目标不再存在。测试已改为直接拦截数据库连接入口，并强制重新生成OpenAPI，继续验证接口文档不读取数据库；没有跳过该测试或恢复无用导入。

## 记录与恢复依据

本次修改前的源码、原有Git状态及逐文件哈希已保存在：

`/data/llm_cpu_gpu_poc/experiments/dead_code_cleanup_20260914/`

主要文件：

- `backend-source-before.tar.gz`、`frontend-source-before.tar.gz`：清理前工作区源码快照，不包含 `.env`。
- `removed-backend.json`、`removed-frontend.json`：删除清单及依据。
- `backend-cleanup.patch`、`frontend-cleanup.patch`、`changed-files.json`：仅本次清理的差异及统计。
- `unit-final.log`、`browser-tests.log`、`frontend-build.log`、`eslint-final.log`：验证日志。
- `contract-comparison.json`、`retained-function-comparison.json`、`replay-comparison.json`：接口、函数结构及业务结果对照。

若需要回退，应依据这些本次快照恢复相应文件，避免使用整体Git回退覆盖此前已完成、尚未提交的业务修复。
